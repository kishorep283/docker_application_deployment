from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def home(self):
    return HttpResponse("Hello These is basic page that is using for docker deployment learning. Addedd Gunirconr server step, Added Nginx server ")